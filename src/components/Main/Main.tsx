import CardsGrid from '../Cards/CardsGrid';

import styles from './Main.module.css';

const Main = () => {
  return (
    <main className={styles.main}>
      <CardsGrid />
    </main>
  );
};

export default Main;
